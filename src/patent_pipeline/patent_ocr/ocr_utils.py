from pathlib import Path

def doc_to_text(path: Path, lang: str, is_pdf: bool) -> str:
    # ⛳️ Placeholder: remplace par ton vrai OCR (pdf2image + pytesseract + OpenCV)
    return f"[DUMMY_OCR for {path.name} / lang={lang} / pdf={is_pdf}]"
